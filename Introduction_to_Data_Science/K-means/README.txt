k-means.py是实现k-means算法的文件
set.py是用来生成数据集的文件，生成数据集名为dataset.csv
dataset1.csv到dataset3.csv为现有的3个数据集
将任意一个改名为dataset.csv并和k-means-v2.exe(或k-means.exe，k-means-v2.py，k-means.py)放在同一目录下运行即可实现聚类
聚类是逐步迭代的，关闭上一次迭代窗口即可生成下一次迭代窗口，直到生成最终结果